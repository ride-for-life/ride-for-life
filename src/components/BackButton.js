import React from 'react'

const button = {
    cursor: 'pointer',
    background: 'none',
    border: 'none',
    color: '#78849E',
    width: '41px',
    height: '13px',
    margin: '70px 0'
}

const BackButton = () => {
    return (
        <button style={button}>
        BackButton
        </button>
    );
};

export default BackButton;
